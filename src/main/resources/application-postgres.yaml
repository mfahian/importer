spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/importer
    username: admin
    password: admin
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: true
